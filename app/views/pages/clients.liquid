{% extends 'layout' %}
{% block 'main' %}
<div id="content" role="main">
  <div id="banner">
    <div class="inner">
      <div class="center bannerInside">
        <div class="bannerExtend bannerLeft"></div>
        <div class="bannerExtend bannerRight"></div>
        <div class="row-fluid bannerContent">
          <div class="span5 imageContent">
            <img src="{{ 'images/banner/customer.png' | theme_image_url }}" alt=""/>
          </div>
          <div class="span7 textContent">
            {% block 'Bandeau' %}
            <article class="text">
              <header class="hd">
                <h2 class="title02">{% editable_short_text 'Titre', priority: 100 %}Ils se sont pris au jeu{% endeditable_short_text %}</h2>

                <p class="subTitle02">{% editable_short_text 'Sous-Titre', priority: 90 %}Les participants{% endeditable_short_text %}
              </header>
              <div class="bd">
                {% editable_long_text 'Texte bandeau', hint: 'Utiliser les balises p, ul/li, strong, a sans classe.', priority: 80 %}
                <p>
                  Follow The Sun s’adresse aux entreprises enseignes ou marques désireuses de
                  développer une stratégie de Gestion de la Relation Client et s’accompagnant de la
                  création d’une base de données multi source provenant des différents systèmes
                  existant au sein des entreprises concernées.
                </p>

                <p>
                  L’entreprise Follow The Sun entretient des relations suivies et dynamiques avec ses
                  clients par l’intermédiaire de son écosystème de partenaires.
                </p>
                {% endeditable_long_text %}
              </div>
            </article>
            {% endblock %}
          </div>
        </div>
      </div>
    </div>
  </div>
  <div id="mainContent" class="center">
    <h2 class="title02">{% editable_short_text 'Titre', priority: 10 %}Nos Clients{% endeditable_short_text %}</h2>

    <p class="subTitle02">{% editable_short_text 'Sous-Titre', priority: 9 %}Les joueurs{% endeditable_short_text %}</p>

    <ul class="customersBloc">
      {% for customer in contents.customers %}
        <li><img src="{{ customer.logo['url'] }}" alt="{{customer.name}}"/></li>
      {% endfor %}
    </ul>

  </div>
  <div id="bottomContent">
    {% block 'Témoignages clients' %}
    <div class="center">
      <div class="customersQuotation">
        <div class="hd">
          <h3 class="title02">{% editable_short_text 'Titre', priority: 10 %}Ils en parlent mieux que nous{% endeditable_short_text %}</h3>

          <p class="subTitle02">{% editable_short_text 'Sous-Titre', priority: 9 %}Expériences{% endeditable_short_text %}</p>
        </div>
        <div class="bd">
          <ul class="row-fluid quotationList">
            {% with_scope type:client %}
              {% for testimony in contents.testimonials limit:3 %}
                <li class="span4 {% if forloop.first %}boxArrow{% endif %}">
                  <div class="xtraPaddingL">
                    <blockquote>
                      « {{testimony.description}} ».
                    </blockquote>
                    <p>
                      <strong>{{testimony.author}}</strong><br/>
                      <span class="job">{{testimony.company}}</span>
                    </p>
                  </div>
                </li>
              {% endfor %}
            {% endwith_scope %}
          </ul>
        </div>
        <div class="ft more">
          <a href="/contact?title=Client" class="btn btn03"><span>Nous contacter</span></a>
        </div>
      </div>
    </div>
    {% endblock %}
  </div>
</div>


{% endblock %}